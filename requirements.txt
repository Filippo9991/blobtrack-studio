# ── Profilo LITE (default) ─────────────────────────────────────────────────
# Tutto ciò che serve per l'app web + blob detection "color" con OpenCV.
# È il profilo usato per il deploy (Render free tier, 512MB RAM).
#
# Per il profilo FULL in locale (YOLO, MediaPipe, reattività audio):
#     pip install -r requirements-local.txt
# L'app rileva da sola cosa è installato (engine.capabilities()) e adatta la UI.

# Web
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.3.0
WTForms==3.2.2
email-validator==2.3.0
python-dotenv==1.2.2
requests==2.34.2
gunicorn==26.0.0

# Database produzione (PostgreSQL su Render; in locale si usa SQLite, incluso in Python)
psycopg2-binary==2.9.12

# Computer vision (motore `engine/`)
opencv-contrib-python-headless==4.13.0.92
numpy==2.4.6
pydantic==2.13.4

# Test
pytest==9.0.3
