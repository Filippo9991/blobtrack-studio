# Python
venv/
__pycache__/
*.pyc
*.pyo

# Flask / DB
instance/
*.db
*.sqlite3

# Secrets
.env

# Uploads (runtime, ephemeral) — cartella sotto app/ dopo il refactor
app/static/uploads/*
!app/static/uploads/.gitkeep

# Archived legacy project (not deployed, kept local only)
legacy/
# Esperimento legacy in Dart (Supabase+OpenAI), estraneo al progetto d'esame
supabase_ai/

# OS
.DS_Store

# Claude Code local settings
.claude/

# YOLO weights: solo yolov8n (default leggero) è committato; gli altri
# (yolov8 m/x, tutti gli YOLO11) si auto-scaricano a runtime.
yolov8m.pt
yolov8x.pt
yolo11*.pt

# Recap di lavoro (locale, non parte del repo d'esame)
RECAP.md
HANDOFF.md
GUIDA_PROGETTO.md
